import React from 'react';
import Layout from '../Layout/layout'
import CropList from '../../component/CropList'
const Crop = () => {
    return (
        <Layout>
            <CropList/>
        </Layout>
    );
};

export default Crop;
